sqlalchemy>=2.0
fsspec>=2024.1
pyarrow>=14.0
deltalake>=0.15
pyyaml>=6.0
requests>=2.28
# Dialect drivers — install as needed per source:
# pip install psycopg2-binary       # PostgreSQL
# pip install pymysql                # MySQL
# pip install pyodbc                 # MSSQL / Azure SQL / Fabric SQL Database via ODBC
# pip install oracledb               # Oracle (thin mode, no Instant Client)
# pip install snowflake-sqlalchemy   # Snowflake
# pip install sqlalchemy-bigquery    # BigQuery
# pip install sqlalchemy-redshift    # Redshift
# Cloud storage drivers — install as needed:
# pip install s3fs                   # AWS S3
# pip install adlfs                  # Azure ADLS / OneLake
# pip install gcsfs                  # Google Cloud Storage
# File format extras — install as needed:
# pip install fastavro               # Avro
# pip install openpyxl               # Excel (.xlsx)
# Lakehouse catalog drivers — install as needed:
# pip install 'pyhive[hive]'         # Hive Metastore (PyHive + Thrift)
# pip install trino                  # Iceberg via Trino
