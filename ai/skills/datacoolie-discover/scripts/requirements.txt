# datacoolie-discover optional dependencies
# All packages below are import-on-demand — install only what you need.

# Core (no extra dependencies — uses stdlib csv module)

# --- Database introspection ---
sqlalchemy>=2.0          # Core DB abstraction (required for --source-type db)
# Dialect drivers (install one per target DB):
# psycopg2-binary       # PostgreSQL
# pymysql               # MySQL
# pyodbc                # MSSQL
# oracledb              # Oracle
# snowflake-connector-python   # Snowflake
# sqlalchemy-bigquery   # BigQuery

# --- Cloud auth (for token-based DB/storage auth) ---
# boto3                 # AWS IAM tokens (RDS, Glue, S3)
# azure-identity        # Azure AD tokens (MSSQL, ADLS, Fabric)
# azure-storage-file-datalake  # OneLake DFS SDK (required for OneLake paths)
# google-auth           # GCP ADC (BigQuery, GCS)

# --- File storage backends ---
# fsspec>=2023.1        # Unified filesystem interface (required for remote paths)
# s3fs>=2023.1          # AWS S3
# adlfs>=2023.1         # Azure Blob / ADLS Gen2 / OneLake
# gcsfs>=2023.1         # Google Cloud Storage
pyarrow>=12.0            # Parquet schema reading + HDFS
# openpyxl              # Excel (.xlsx) schema reading

# --- API discovery ---
# requests              # HTTP calls (sample API calls, OAuth2 token exchange)
# lxml                  # OData $metadata XML parsing
# gql                   # GraphQL introspection (optional, can use raw requests)

# --- Lakehouse catalog APIs ---
# databricks-sdk        # Databricks Unity Catalog
# pyiceberg             # Apache Iceberg REST Catalog
# deltalake             # Delta Lake standalone (schema from path)
# PyHive                # Hive Metastore (thrift)
